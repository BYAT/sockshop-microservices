apiVersion: tekton.dev/v1beta1 
kind: Pipeline
metadata:
  name: test-pipeline
spec:
  resources: 
    - name: user-resource
      type: git
  tasks:
    - name: test-pipe
      taskRef:               # will start execution immediately
        name: test-task
      resources:
        inputs:
        - name: user-resource
          resource: user-resource
